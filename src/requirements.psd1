@{
    # Modules bundled with the PowerShell Language Worker
    'Microsoft.PowerShell.Archive' = @{
        Version = '1.1.0.0'
        Target = 'src/Modules'
    }
    'ThreadJob' = @{
        Version = '1.1.2'
        Target = 'src/Modules'
    }
}
